language: julia

os:
  - linux
  - osx

julia:
  - 1.0
  - nightly

notifications:
  email: andreas@noack.dk

after_success:
  - julia -e 'cd(Pkg.dir("Cointegration")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder())'
